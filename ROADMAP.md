# CareerAgent 开发路线

> 规划日期：2026-09-18  
> 当前状态：V0.9a 已完成并合并到本地 `main`；V0.9b 已完成本地实现与验证，等待 Git 提交
> 说明：V0.9a 已实现；V0.9b—V1.4 仍是计划，不能提前记录为已完成功能。

## 产品目标

CareerAgent 将从学习建议原型逐步升级为可解释、可验证、可评估的求职辅助 Agent：

```text
输入脱敏 JD
→ 读取个人真实证据和学习进度
→ Python 校验并计算匹配关系
→ Agent 调用受控工具生成职位定制建议
→ Python 核对结论和来源
→ 输出能力缺口、学习任务和面试问题
→ 用户确认后更新学习或投递状态
```

系统始终区分 JD 要求、已验证能力、正在学习的内容、无法确认的能力、模型建议和实际写入操作。

## 设计原则

1. **真实证据优先**：学习计划不能作为已掌握证据；“已经具备”必须关联可核对的证据 ID。
2. **Python 控制，模型生成**：Python 负责验证、权限、匹配、来源和写入；模型负责解释和组织建议。
3. **先离线，再接模型**：新能力先使用固定数据和离线测试跑通，再决定是否进行真实模型验收。
4. **写入必须受控**：完整提案、Python 校验、精确 `CONFIRM`、实际写入和磁盘回读缺一不可。
5. **开发与学习分开**：Codex 协助实现不等于用户已经独立掌握相应知识。

## 当前 V0.8.1

V0.8.1 是 V0.8 的校验补丁版本：

- 拒绝空白目标岗位。
- 拒绝空白的现有文本学习进度。
- 拒绝使用空白文本覆盖已有进度。
- 49 项离线测试通过，测试未修改 `users.json` 或 `study_progress.json`。

当前版本仍不包含 JD 匹配、FastAPI、SQLite、图形界面、RAG、多 Agent、自动岗位搜索或自动投递。

## V0.9a：岗位与个人证据数据契约

V0.9a 不调用大模型，先建立可信的数据结构和校验基础。

计划输入：

- `jobs.json`：脱敏岗位、要求 ID、技能 ID、类别和优先级。
- `candidate_evidence.json`：脱敏用户、证据 ID、技能 ID、能力层级、来源和验证状态。
- `study_progress.json`：正在学习但不能自动视为已掌握的内容。

计划函数：

```text
validate_job
validate_candidate_evidence
```

V0.9a 完成需要：两个脱敏演示 JSON、数据校验函数和离线测试实际通过；测试不得修改项目数据。该阶段不实现匹配、不调用真实模型，也不产生 API 费用。

## V0.9b：本地可解释 JD 匹配

V0.9b 在 V0.9a 的合法数据基础上实现确定性匹配，匹配状态由 Python 决定。

计划函数：

```text
get_job_requirements
get_candidate_evidence
match_job_requirements
```

每项岗位要求只能进入一种状态：

- `matched`：存在直接且已验证的证据。
- `partial`：存在相关证据，但深度或场景不足。
- `missing`：岗位明确要求，但当前没有相关证据。
- `unverified`：存在相关描述，但证据尚未确认。

确定性判定矩阵：

| 相同 `skill_id` 的证据情况 | 状态 | 判定依据 |
|---|---|---|
| 没有任何相关证据 | `missing` | 当前数据中没有可引用的能力证据。 |
| 有相关证据，但全部为 `verified=false` | `unverified` | 只能证明存在相关描述，不能证明能力已经核实。 |
| 至少一条已验证证据，最高层级为 `learning` 或 `practice` | `partial` | 已有学习或练习证据，但不足以证明项目级应用。 |
| 至少一条已验证证据，层级为 `project` 或 `production` | `matched` | 存在直接、已验证且达到项目级或生产级的证据。 |

矩阵执行规则：

1. 匹配前必须先通过 V0.9a 的岗位和候选人证据校验；非法输入应整体拒绝，不能返回部分匹配结果。
2. 只比较完全相同的 `skill_id`，不使用描述文本进行模糊猜测。
3. 多条相关证据按最高的已验证层级决定状态，优先级为 `matched > partial > unverified > missing`。
4. `verified=false` 的 `project` 或 `production` 描述不能提升状态；若同时存在已验证的 `practice`，结果仍为 `partial`。
5. `category` 和 `priority` 只作为岗位元数据原样保留。当前 Schema 没有岗位最低能力层级字段，因此不能用 `required` 或 `preferred` 擅自推断熟练度门槛。
6. 每项输出保留岗位要求字段、唯一状态和按输入顺序排列的 `related_evidence_ids`；没有证据时该列表为空。
7. 输出顺序必须与岗位要求顺序一致，便于审计和稳定测试。

两个只读工具的契约：

- `get_job_requirements(job_id, jobs_file)` 只返回一个已验证岗位的 `job_id`、`title` 和 `requirements`。
- `get_candidate_evidence(username, evidence_file)` 只返回一个已验证候选人的 `username` 和 `evidence`。
- 两个工具都先验证非空查询值，再读取 UTF-8 JSON，并复用 V0.9a 根结构校验。
- 文件缺失、JSON 损坏、根结构非法或目标不存在时返回 `ok=false` 和明确错误，不向调用者抛出文件或 JSON 异常。
- 文件路径由 Python 控制层提供；后续模型工具 Schema 不得允许模型指定任意本地路径。
- 工具全程只读，不修改岗位、证据或学习进度数据。

V0.9b 完成需要：两个只读工具与匹配函数实际运行、新旧离线测试通过、真实数据不变、README 和脱敏输出示例完成。该版本仍不调用真实模型，也不产生 API 费用。

## V1.0：JD 分析 Agent 化

在 V0.9 确定性匹配基础上，让千问通过受控只读工具读取岗位要求、个人证据和学习进度。匹配状态仍由 Python 计算，模型只能解释结果、生成学习任务和面试问题。

必须拒绝跨用户、跨岗位、未授权工具、虚构来源 ID、篡改匹配状态和非法输出结构。真实千问验收只有在用户明确同意费用后进行。

## V1.1：评估与安全基线

- 建立至少 12 个脱敏案例：正常、数据错误或冲突、提示词注入或越权。
- 评估结构通过率、来源准确率、匹配一致性、虚构率和安全拒绝率。
- 将 JD 始终视为待分析数据，不把其中的“忽略规则”“读取 `.env`”或“执行写入”等内容当成系统指令。
- 模型不可用时保留 V0.9 本地结果，不写文件、不伪装成功、不无限重试。

## V1.2：FastAPI 后端

计划将核心逻辑封装为本地 HTTP API。预计使用 FastAPI、Pydantic、Uvicorn 和 HTTP 测试客户端；安装前必须说明用途、影响、网络行为和无框架替代方案，并获得用户确认。

API 不能绕过写入提案与确认，也不能允许客户端传入任意本地文件路径。完成时只能描述为本地服务，不能宣称已经云部署。

## V1.3：SQLite 投递追踪

优先使用 Python 自带的 `sqlite3`，保存岗位、投递状态、事件和下一步行动。状态更新仍需提案、原状态和新状态预览、精确确认、事务写入、查询复核和事件记录。

测试必须使用临时数据库；公开仓库不提交真实投递数据。

## V1.4：项目展示界面

优先考虑 Streamlit，但新增依赖前仍需用户确认。计划包含 JD 分析、学习进度和投递追踪页面；界面不能绕过核心校验和受控写入。

演示只使用脱敏数据，不显示密钥、`.env`、完整本地路径或真实投递隐私。

## V2.0 候选能力

以下内容不属于 V0.9—V1.4 的承诺范围：

- RAG：只在资料规模明显增加、结构化 JSON 难以维护且已有检索评估方法时考虑。
- 多 Agent：只在单 Agent 存在明确且可测的问题，并能证明质量提升时考虑。
- 自动岗位搜索：先解决数据来源、网站规则、岗位时效和去重问题。
- 自动投递：当前不建议，账号、隐私和误操作风险高于收益。
- 云部署：本地功能、评估、安全和隐私边界稳定后再决定。

## 统一完成标准

一个版本只有同时满足以下条件，才能标记完成：

1. 范围明确，代码已经实现并实际运行。
2. 自动化测试实际执行并通过，原有功能没有退化。
3. 测试没有修改真实用户数据。
4. 真实模型验收在需要时获得用户许可，并与模型替身测试明确区分。
5. README、`PROJECT_STATUS.md` 和 `CAREER_AGENT_HISTORY.md` 已同步。
6. 脱敏示例或截图已核对，没有密钥和隐私泄露。
7. Git 差异已经检查；只有用户明确授权后才提交、合并或推送。

创建计划、创建文件、通过语法检查或显示在界面中，都不能单独算作版本完成。

## 统一 Git 流程

```text
检查状态
→ 创建版本功能分支
→ 编写测试
→ 实现当前版本
→ 运行目标测试和完整测试
→ 检查真实数据与代码差异
→ 更新文档并再次测试
→ 用户验收
→ 用户授权后提交、合并或推送
```

不要混合多个版本、删除失败测试来获得绿色结果、盲目重跑失败测试，或提交 `.env`、真实数据库和私人学习记录。

## 预计周期

| 阶段 | 计划时间 |
|---|---:|
| V0.8.1 验收与基线同步 | 0.5 天 |
| V0.9a 数据契约与校验 | 1—2 天 |
| V0.9b 本地 JD 匹配 | 2—3 天 |
| V1.0 JD 分析 Agent | 3—5 天 |
| V1.1 评估与安全 | 3—4 天 |
| V1.2 FastAPI 后端 | 3—5 天 |
| V1.3 SQLite 投递追踪 | 3—5 天 |
| V1.4 展示界面 | 3—5 天 |
| 文档、演示与简历整理 | 2—3 天 |

整体约 4—6 周，按学习状态、测试结果和秋招安排调整，不为追赶版本号跳过验收。

## 与 Hello-Agents 的学习映射

| 开发版本 | 学习重点 | 学习验收 |
|---|---|---|
| V0.9a | 事实、数据结构与验证 | 能解释为什么学习计划不能直接当成能力证据 |
| V0.9b | 规则与确定性匹配 | 能解释为什么匹配状态先由 Python 确定 |
| V1.0 | LLM、工具和控制程序 | 能闭卷说明谁决策、谁执行、谁校验 |
| V1.1 | Agent 评估与安全 | 能说明“能运行”和“可信”的区别 |
| V1.2 | API 输入输出边界 | 能解释请求、响应和数据校验 |
| V1.3 | 状态、持久化与事务 | 能解释状态更新和失败回滚 |
| V1.4 | 产品交互和系统分层 | 能解释界面为何不能绕过安全逻辑 |

项目实现不能自动作为学习掌握证据。

## 下一步

1. 完成 V0.9b 的最终差异、安全和脱敏输出检查。
2. 用户确认是否提交已经验证的 V0.9b；合并和推送仍分别等待明确授权。
3. V1.0 开始前重新确认真实模型费用、工具权限和密钥边界。
4. V1.0 才将两个只读工具与确定性匹配结果接入 Agent 流程；模型仍不能决定或篡改匹配状态。
